{
  "name": "lewix-watch-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "setup": "python3 -m venv .venv && .venv/bin/pip install cadquery-ocp trimesh numpy scipy",
    "model": ".venv/bin/python scripts/build_model.py"
  },
  "dependencies": {
    "gsap": "^3.15.0",
    "lenis": "^1.3.26",
    "three": "^0.169.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
